<nav class="navbar navbar-default navbar-fixed">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navigation-example-2">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('lstats_user_homepage') }}">Home</a>
        </div>
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav navbar-left">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                        <i class="fa fa-search"></i>
                        <b class="caret hidden-sm hidden-xs"></b>
                    </a>
                    <ul class="dropdown-menu">
                        <li>
                            <form class="navbar-form" role="search" action="{{ path('lstats_summoner_search') }}">
                                <div class="input-group">
                                    <input type="search" class="form-control" placeholder="Summoner statistics..." name="srch-term" id="srch-term">
                                    <div class="input-group-btn">
                                        <select class="form-control" id="server-choice" name="server">
                                            {% for key, server in constant('LSTATS\\RiotBundle\\Services\\ChampionsService::SERVERS') %}
                                                <option value="{{ server }}" >{{ key }}</option>
                                            {% endfor %}
                                        </select>
                                    </div>
                                    <div class="input-group-btn">
                                        <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
                                    </div>
                                </div>
                            </form>
                        </li>
                        <li>
                            <form class="navbar-form" role="search" action="{{ path('lstats_game_search') }}">
                                <div class="input-group">
                                    <input type="search" class="form-control" placeholder="Summoner in game..." name="srch-term" id="srch-term">
                                    <div class="input-group-btn">
                                        <select class="form-control" id="server-choice" name="server">
                                            {% for key, server in constant('LSTATS\\RiotBundle\\Services\\ChampionsService::SERVERS') %}
                                                <option value="{{ server }}" >{{ key }}</option>
                                            {% endfor %}
                                        </select>
                                    </div>
                                    <div class="input-group-btn">
                                        <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
                                    </div>
                                </div>
                            </form>
                        </li>
                    </ul>
                </li>
            </ul>

            <ul class="nav navbar-nav navbar-right">
                {% if is_granted("IS_AUTHENTICATED_FULLY") %}
                    <li>
                        <a class="link" href="{{ path('fos_user_security_logout') }}">
                            <i class="pe-7s-close hide-desktop"></i>
                            <p>Log out</p>
                        </a>
                    </li>
                {% else %}
                    <li>
                        <a class="link" href="{{ path('fos_user_security_login') }}">
                            <p>Login</p>
                        </a>
                    </li>
                    <li>
                        <a class="link" href="{{ path('fos_user_registration_register') }}">
                            <p>Register</p>
                        </a>
                    </li>
                {% endif %}
                <li class="separator hidden-lg hidden-md"></li>
            </ul>
        </div>
    </div>
</nav>